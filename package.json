{
  "name": "mini-tweeter",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "node src/seedData.js",
    "start": "node src/seedData.js && node src/app.js",
    "dev": "nodemon src/app.js",
    "build": "npm install -g node-pre-gyp && npm rebuild bcrypt --build-from-source"
    "start": "node src/app.js",  
    "dev": "nodemon src/app.js",  
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "express-session": "^1.18.1",
    "mongoose": "^8.14.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}